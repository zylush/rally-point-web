VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Leave blank to run in local demo mode (seeded mock data, no network).

# Optional: PayMongo public key (live e-wallet wiring still needs an Edge Function secret)
# VITE_PAYMONGO_PUBLIC_KEY=
