# Canonical staging fingerprint: TDD evidence

Date: 2026-09-23. Journey: a release operator compares the linked Rally-Point-Database staging schema with a twelve-version disposable local clone and must not see false drift merely because the sessions have different `search_path` values.

The original read-only query returned 605 rows in each environment but different marker digests: staging `7940073bdd45109566df7e41c87a529c`, local `78f8c6f0318ecd2017cb92e9969718ac`. Object-level comparison showed twenty differences (plus the marker) in pre-existing policies, the `profiles_id_fkey` definition, and the Auth signup trigger. Staging's path was `"$user", public, extensions`; the local clone's included `auth`. Example: the same policy deparsed `auth.uid()` versus `uid()`, and the same foreign key deparsed `auth.users` versus `users`.

RED: `npm test -- tests/tenant-migration-phases.test.ts` failed the new canonical-path assertion. A second package guard initially failed for a test-selector mistake; after correcting the selector, it failed for the intended missing source hashes. GREEN: `npm test -- tests/privacy-release-package.test.ts tests/tenant-migration-phases.test.ts` passed 22/22. `schema_fingerprint.sql` now pins the session-only path to `public, extensions`; the packager hashes all four shipped check sources.

The canonical query then returned 605 identical rows between linked staging and `rally_prerepair_restore_20260923` (a local twelve-version clone, not a current rollback backup), with marker MD5 `7940073bdd45109566df7e41c87a529c` and zero missing/changed objects. The ordered migration-version lists were identical: `001`, `002`, `003`, `004`, `20260803125450`, `20260805094557`, `20260921090000`, `20260921110828`, `20260922011918`, `20260922163027`, `20260922163830`, `20260922164128`. The separate read-only public relation grant/RLS snapshot matched all five sections: 393 ACL entries, 0 column ACL entries, 528 effective privilege entries, 22 RLS rows, and 12 versions.

Full checks after sealing r7: 182/182 tests pass in 22 files; coverage 90.22% statements, 80.44% branches, 86.74% functions, 92.82% lines. Lint, explicit `tsc --noEmit`, ordinary build, and high-severity dependency audit pass (zero vulnerabilities). The local comparison does not prove current row contents, Auth/Storage history, private-schema completeness, or fresh backup restorability. No database row/schema mutation, staging migration, deployment, enforcement, commit, or push occurred in this verification.
